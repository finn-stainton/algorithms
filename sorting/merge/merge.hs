
main :: IO()
main do 
    print ("Merge sorting:")

    -- let n = q - p + 1;
    -- let m = r - q;

    -- let L[1..n+1]
    -- let R[1..m+1]

    -- for (i = 1 to n) {
    --     L[i] = A[p + i - 1];
    -- }
       
    
    -- for j = 1 to m
    --     R[j] = A[q + j];

    -- L[n + 1] = infinite;
    -- R[m + 1] = infinite;
    -- i = 1;
    -- j = 1;
    -- for k = p to r
    --     if L[i] <= R[j]
    --         A[k] = L[i];
    --         i = i + 1;
    --     else A[k] = R[j];
    --         j = j + 1;
